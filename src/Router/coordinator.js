export const goHomePage = (navigate) => {
    navigate("/")
}

export const goPokedexPage = (navigate) => {
    navigate("/pokedex")
}

export const goPagDetalhes = (navigate, id) => {
    navigate(`/detailsPage/${id}`)
}

